# Copy this file to .env and fill in real values. .env is gitignored.

# Needed from Phase B onward (agent LLM calls)
GROQ_API_KEY=

# Optional — only if/when the AWS bonus deploy happens
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
